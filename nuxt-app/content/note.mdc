# Welcome to the Note

```typescript showLineNumbers
const x = { name: 'Fodo'};

console.log({x});

```

::card
The content of the card
::


::hero
Default slot text

#description
This will be rendered inside the `description` slot.
::